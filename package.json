{
  "name": "plugma_test",
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:plugma": "pnpm --filter plugma run dev",
    "dev:server": "pnpm --filter server run dev",
    "dev": "concurrently \"pnpm run dev:plugma\" \"pnpm run dev:server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  }
}